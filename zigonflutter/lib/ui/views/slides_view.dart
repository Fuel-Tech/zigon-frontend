import 'package:flutter/material.dart';

class SlidesView extends StatelessWidget {
  const SlidesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
